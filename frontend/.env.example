# ============================================
# Vibehuntr Frontend Environment Variables
# ============================================

# Backend API URL
# Development: http://localhost:8000
# Production: https://your-backend-url.run.app
VITE_API_URL=http://localhost:8000

# ============================================
# Optional: Debug Configuration
# ============================================
# Enable debug logging in browser console
VITE_DEBUG=false

# ============================================
# Link Preview Configuration
# ============================================
# Enable/disable link preview feature in the frontend
# When disabled, URLs will be displayed as plain text without preview cards
# Default: true
VITE_LINK_PREVIEW_ENABLED=true

# ============================================
# Notes
# ============================================
# - All environment variables MUST be prefixed with VITE_ to be exposed to the client
# - Changes to .env require restarting the dev server (npm run dev)
# - Never commit .env files with real API keys to version control
# - Use .env.local for local overrides (automatically ignored by git)
#
# Link Preview Feature:
# - VITE_LINK_PREVIEW_ENABLED controls whether link preview cards are rendered
# - Backend configuration (timeout, cache TTL, excluded domains) is managed in backend/.env
# - When disabled, URLs in messages will appear as plain text
# - This setting only affects the frontend rendering; backend API remains available
